export type BlockchainType = {
    id: number
    prid: number
    pruuid: string
    user_id: number
    chain: string
    contract: string
    txid: string
    account: string
    fingerprint: string
    gas_used: Number
    qrcode: string
    created_at: Date
    stripped: string
    mp3: string
    redirect_url: string
    incoming: string
    hitcount: number
    self_host_domain: string
}